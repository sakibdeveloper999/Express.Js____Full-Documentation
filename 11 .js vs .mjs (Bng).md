 `.mjs` কী, কেন ব্যবহার করা হয়, `.js` থেকে কীভাবে আলাদা, এবং Express.js-এ এটি কীভাবে কাজ করে ধাপে ধাপে উদাহরণসহ।

---

## 🧩 `.mjs` কী?

`.mjs` মানে হলো **Module JavaScript**।
এটি এমন একটি ফাইল এক্সটেনশন যা **Node.js**-কে বলে দেয় যে এই ফাইলটি **ES Modules (ECMAScript Modules)** সিনট্যাক্স ব্যবহার করছে।
অর্থাৎ — এখানে `import` এবং `export` ব্যবহার করা হয়েছে।

আগে Node.js-এ ডিফল্টভাবে **CommonJS (CJS)** সিস্টেম ব্যবহার করা হতো, যেখানে `require` এবং `module.exports` ব্যবহার করা হয়।

---

## 🆚 `.js` এবং `.mjs` এর পার্থক্য

| বিষয়              | `.js` (CommonJS)                      | `.mjs` (ES Module)               |
| ----------------- | ------------------------------------- | -------------------------------- |
| মডিউল সিস্টেম     | CommonJS                              | ES Modules                       |
| Import সিনট্যাক্স | `const express = require('express');` | `import express from 'express';` |
| Export সিনট্যাক্স | `module.exports = app;`               | `export default app;`            |
| Node.js বোঝে      | ডিফল্টভাবে CJS                        | স্বয়ংক্রিয়ভাবে ESM             |
| স্ট্যান্ডার্ড     | পুরনো সিস্টেম                         | আধুনিক (Browser-Compatible)      |

---

## 🧠 কেন `.mjs` তৈরি করা হয়েছে?

Node.js যখন **ES Module** সাপোর্ট যোগ করে, তখন `.js` ফাইলগুলো ইতিমধ্যেই CommonJS সিনট্যাক্সে ব্যবহৃত হচ্ছিল।
তাই বিভ্রান্তি এড়াতে Node.js `.mjs` এক্সটেনশন চালু করে, যেন বোঝা যায় কোন ফাইলটি **ES Module** ব্যবহার করছে।

👉 তাই:

* `.js` → CommonJS হিসেবে ধরা হয়
* `.mjs` → ES Module হিসেবে ধরা হয়

---

## 🧾 Express.js এ `.mjs` ব্যবহারের উদাহরণ

ধরা যাক তুমি Express.js অ্যাপ তৈরি করতে চাও **ES Modules (modern syntax)** দিয়ে।

### 🔹 Step 1: `package.json` তৈরি করো

```bash
npm init -y
```

---

### 🧩 অপশন ১: `.mjs` ফাইল ব্যবহার করা

#### 📁 ফোল্ডার স্ট্রাকচার:

```
project/
│
├── app.mjs
└── package.json
```

#### 🧠 `app.mjs` কোড:

```js
import express from 'express';

const app = express();
const port = 3000;

// Middleware
app.use(express.json());

// Route
app.get('/', (req, res) => {
  res.send('Hello, Express using ES Modules (.mjs)');
});

// Server run
app.listen(port, () => {
  console.log(`Server running at http://localhost:${port}`);
});
```

#### 🏃 চালাও:

```bash
node app.mjs
```

✅ কাজ করবে কারণ `.mjs` ফাইল Node.js-কে বলে দেয় — “এটি একটি ES Module ফাইল”।

---

### 🧩 অপশন ২: `.js` ফাইল কিন্তু `"type": "module"`

যদি `.mjs` ফাইল ব্যবহার করতে না চাও, তবে `package.json` ফাইলে এই লাইনটি যোগ করো:

```json
{
  "type": "module"
}
```

এখন তোমার `.js` ফাইলও ES Module হিসেবে কাজ করবে।

#### `app.js`

```js
import express from 'express';

const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send('Hello, Express using ES Modules (.js with type:module)');
});

app.listen(port, () => {
  console.log(`Server running on port ${port}`);
});
```

চালাও:

```bash
node app.js
```

✅ একইভাবে কাজ করবে, তবে `.js` ফাইলেই।

---

## ⚙️ `.mjs` ফাইলে যা সম্ভব (ESM Features)

1. **import / export** — নেটিভ সিনট্যাক্স

   ```js
   import express from 'express';
   export const router = express.Router();
   ```

2. **Top-level await** — `await` ব্যবহার করা যায় ফাংশনের বাইরে

   ```js
   const data = await fetch('https://api.example.com');
   ```

3. **require / module.exports** ব্যবহার করা যায় না
   এগুলোর পরিবর্তে `import` ও `export` ব্যবহার করতে হবে।

4. **File path অবশ্যই সম্পূর্ণ দিতে হবে**
   যেমন:

   ```js
   import userRoute from './routes/user.js';
   ```

   এখানে `.js` এক্সটেনশন না দিলে কাজ করবে না।

---

## 💡 কখন `.mjs` ব্যবহার করবে?

✅ ব্যবহার করো যখন:

* একই প্রজেক্টে CommonJS আর ES Module দুটোই আছে।
* `package.json` পরিবর্তন করতে চাও না।
* আলাদা ভাবে মডার্ন ও লেগেসি ফাইল আলাদা রাখতে চাও।

---

## ⚡ বাস্তব উদাহরণ: Express + MongoDB (`.mjs` সহ)

```bash
npm install express mongoose
```

📁 ফোল্ডার:

```
project/
│
├── server.mjs
└── package.json
```

`server.mjs`:

```js
import express from 'express';
import mongoose from 'mongoose';

const app = express();
const port = 3000;

// MongoDB সংযোগ
await mongoose.connect('mongodb://127.0.0.1:27017/testDB');
console.log('✅ Connected to MongoDB');

// Middleware
app.use(express.json());

// Route
app.get('/', (req, res) => {
  res.send('Hello from Express + MongoDB using .mjs');
});

app.listen(port, () => {
  console.log(`🚀 Server running on http://localhost:${port}`);
});
```

চালাও:

```bash
node server.mjs
```

---

## ⚠️ সাধারণ ভুল ও সমাধান

| সমস্যা                                                      | কারণ                          | সমাধান                                         |
| ----------------------------------------------------------- | ----------------------------- | ---------------------------------------------- |
| `SyntaxError: Cannot use import statement outside a module` | Node জানে না এটি ESM          | `.mjs` ব্যবহার করো অথবা `"type": "module"` দাও |
| `Error: Must use import to load ES Module`                  | CommonJS ফাইল থেকে ESM কল করা | সব ফাইল ESM করো                                |
| `ERR_MODULE_NOT_FOUND`                                      | ফাইল এক্সটেনশন নেই            | `.js` এক্সটেনশন যোগ করো                        |

---

## ✅ সারাংশ

| বিষয়                 | বর্ণনা                                                |
| -------------------- | ----------------------------------------------------- |
| `.mjs` মানে          | Module JavaScript                                     |
| কাজ                  | Node.js-কে বলে দেয় — এই ফাইলটি ES Module             |
| সিনট্যাক্স           | `import` / `export`                                   |
| সবচেয়ে ভালো ব্যবহার | Express.js, EJS, MongoDB, আধুনিক backend প্রজেক্ট     |
| বিকল্প               | `"type": "module"`                                    |
| সুবিধা               | মডার্ন জাভাস্ক্রিপ্ট, ব্রাউজার কম্প্যাটিবল, ক্লিন কোড |

---
